{ x = 20 }
RENAMING(x|y)
  var i in
    i := 10;
    var i in
      i := 0;
      i := i + 1;
      assert (i = 0)
    end;
    assume ~ (i < 0);
    assert (i = 0 ==> true)
  end;
  assert x = 20;
  var i in
    i := 10;
    assert i = 10
  end
{ x - 1 = 0 }
