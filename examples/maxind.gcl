{ i < n }
MAXIND(a, i, n | r)
  var max in
    s, max, r := i, a[i], i;
    while i < n do
      if a[i] > max then
        max, r := a[i], i
      else
        skip
      fi;
      i++
    end
  end
{ (forall j :: j in [s..n) ==> a[r] >= a[j]) }
