{ i < n }
PRENEX(a, i, n | r)
  var min in
    min, r := a[i], i;
    while i < n do
      if a[i] < min then
        min, r := a[i], i
      else
        skip
      fi;
      i++
    end
  end
{ (forall j :: i <= j /\ j < n ==>
    (forall k :: i <= k /\ k < n ==>
      a[r] <= a[j] /\ a[r] <= a[k])) }
